<!doctype html>
<html lang="en" class="bg-pri-800 antialiased" x-data>

  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>{%- if title -%}{{ title +' : ' }}{%- endif -%} {{ site.meta.title +' : '+ site.meta.sub }}</title>
    <meta name="description" content="{{ site.meta.description }}" />
    <meta name="author" content="{{ site.meta.author + ' - '+ site.meta.author_url }}" />
    <meta name="generator" content="{{ eleventy.generator }}" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Outfit:wght@100..900&display=swap" />

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <meta property="og:title" content="{{ site.meta.title }}" />
    <meta property="og:description" content="{{ site.meta.description }}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://comx.media{{ page.url }}" />
    <meta property="og:image" content="https://comx.media/apple-touch-icon.png" />

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@algolia/algoliasearch-netlify-frontend@1/dist/algoliasearchNetlify.css" />
  </head>

  <body class="text-pri-400 !block" style="display: none;" data-instant-intensity="mousedown">
    <div class="min-h-screen flex-(& col) relative">
      <header class="w-full flex-none relative z-20{%- if page.url === '/' %} bg-gradient-to-b from-pri-800{%- endif %}">
        <div class="p-6 flex items-center">
          <a href="/" class="group font-semibold flex items-center gap-3 motion-safe:(transition) hover:(text-blue-300)">
            <b class="px-1 py-px bg-white text-pri-800 font-bold motion-safe:(transition) group-hover:(bg-blue-300)">{{ site.meta.title_short }}</b>
            <i class="not-italic uppercase">{{ site.meta.title }}</i>
          </a>
          <nav class="ml-auto" x-data="{menu: false}">
            <ul>
              <li class="relative z-20">
                <a href="#" class="motion-safe:(transition) hover:(text-blue-300)" :class="{'text-blue-300': menu}" @click.prevent="menu = !menu"
                  :aria-expanded="menu"
                  aria-controls="nav-menu"
                >
                  <i x-show="!menu" x-cloak><iconify-icon icon="fa6-solid:bars" inline="false"></iconify-icon><b class="sr-only">Open menu</b></i>
                  <b x-show="menu" class="fixed top-6 right-6" x-cloak><iconify-icon icon="fa6-solid:xmark" inline="false"></iconify-icon><b class="sr-only">Close menu</b></b>
                </a>
              </li>
              <li class="relative z-10">
                <div x-show="menu" @click.outside="menu = false" class="w-1/2 bg-(gray-900 gradient-to-r) from-gray-800 fixed inset-y-0 right-0 shadow-2xl transform md:(w-full max-w-sm)"
                  x-transition:enter="transition ease-out"
                  x-transition:enter-start="opacity-0 translate-x-4"
                  x-transition:enter-end="opacity-100 translate-y-0"
                  x-transition:leave="transition ease-in"
                  x-transition:leave-start="opacity-100 translate-y-0"
                  x-transition:leave-end="opacity-0 translate-x-4"
                  x-cloak
                >
                  <div class="bg-black/30 absolute inset-0"></div>
                  <ul class="mt-4 py-12 text-lg font-black relative md:(text-2xl)" id="nav-menu">
                    {%- for item in collections.all | eleventyNavigation %}
                    <li>
                      <a href="{{ item.url }}" class="group py-3 px-6 flex items-center gap-4 relative motion-safe:(transition) hover:(bg-pri-500/10) text-pri-100) md:(py-6) {% if item.url == page.url or item.url in page.url %} active text-sec-400 font-bold{% else %} text-pri-400{% endif %}"
                        {%- if item.url == page.url or item.url in page.url %} aria-current="page"{%- endif %}
                      >
                        <b class="text-pri-500/50"><iconify-icon icon="{{ item.ico }}" inline="false"></iconify-icon></b>
                        <div>
                          {{ item.key }}
                          <small class="text-(xs sec-700) font-semibold block md:(text-sm)">{{ item.title }}</small>
                        </div>
                        {%- if item.url == page.url or item.url in page.url %}<b class="w-[2px] bg-current absolute inset-y-0 left-0"></b>{%- endif %}
                      </a>
                    </li>
                    {%- endfor %}
                  </ul>
                  <div id="search" class="p-8"></div>
                </div>
              </li>
            </ul>
          </nav>
        </div>
      </header>
      <main class="w-full flex-(& 1) relative z-10">
        <section class="w-full flex">
          {{ content | safe }}
        </section>
      </main>
      <footer class="p-6 text-(sm center pri-500) font-semibold w-full flex-none relative z-10{%- if page.url === '/' %} bg-gradient-to-t from-pri-800{%- endif %} md:(flex items-center justify-between)">
        <p>Collected by <a href="https://{{ site.meta.author_url }}/" class="text-pri-400 motion-safe:(transition) hover:(text-sec-400)">{{ site.meta.author }}</a> : 1992 - {% year %}</p>
        <small class="py-1 block opacity-75">Updated: <time datetime="{{ build.time.raw }}">{{ build.time.formatted }}</time></small>
      </footer>
    </div>

    <div class="bg-grid fixed inset-0"></div>
    <div class="w-full h-[50vh] bg-gradient-to-b from-gray-900 fixed top-0 inset-x-0 md:(via-transparent)" aria-hidden="true"></div>
    <div class="w-full h-[50vh] bg-gradient-to-t from-gray-900 fixed bottom-0 inset-x-0 md:(via-transparent)" aria-hidden="true"></div>

    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@algolia/algoliasearch-netlify-frontend@1/dist/algoliasearchNetlify.js"></script>
    <script type="text/javascript">
      algoliasearchNetlify({
        appId: 'VQQIGN2XJZ',
        apiKey: 'b09b73541f84f48c93d90c9231845764',
        siteId: '8b519fe1-da2a-4b2f-93fa-e69519371cd3',
        branch: 'main',
        selector: 'div#search',
      });
    </script>
    <script src="/_assets/js/_app.js?v={% bust %}"></script>
  </body>

</html>