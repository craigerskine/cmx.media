---
layout: 'layouts/default.njk'
pagination:
  data: misc.series
  size: 1
  alias: seriesCurr
permalink: "misc/{{ seriesCurr | slugify }}/"
eleventyComputed:
  title: "{{ seriesCurr }} : Misc"
---

{%- import 'components/heading.njk' as heading %}
{%- import 'components/item-list.njk' as list %}

<article class="container mx-auto p-6">
  {{ heading.x(seriesCurr) }}
  <ul class="grid(& cols-2) gap-16 lg:(grid-cols-5)">
    {%- for item in misc.items | pluck(seriesCurr, 'series') %}
      {{ list.x(name=item.issue +' / '+ item.release_date,signed=item.signed_by,image='/_assets/img/misc/'+ item.series | slugify +'/'+ item.issue | slugify +'.jpg',loop=loop) }}
    {%- endfor %}
  </ul>
</article>