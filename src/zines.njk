---
layout: 'layouts/default.njk'
eleventyNavigation:
  key: Zines
  order: 2
title: 'Magazines'
---

{%- import 'components/heading.njk' as heading %}

<article class="container mx-auto p-6">
  {{ heading.x(title) }}
  <ul class="grid(& cols-2) gap-4 lg:(grid-cols-3 gap-16)">
    {%- for series in (zines.series) %}
      <li>
        <a href="/zines/{{ series | slugify }}/" class="mx-auto w-full max-w-[250px] text-center block relative">
          <figure>
            <img src="/_assets/img/zines/{{ series | slugify }}/series.jpg?v={% version %}" alt="{{ series }}" class="mx-auto w-full" />
            <figcaption>
              <strong class="truncate">{{ series }}</strong>
              <ul class="text-xs font-mono flex flex-wrap justify-center gap-x-3">
                {%- for item in zines.items | pluck(series, 'series') %}
                  <li>
                    {{ item.issue }}
                  </li>
                {%- endfor %}
              </ul>
            </figcaption>
          </figure>
          <b class="w-6 h-6 bg-white flex items-center justify-center absolute -top-3 -right-3 rounded-full">{{ zines.items | pluck(series, 'series') | length }}</b>
        </a>
      </li>
    {%- endfor %}
  </ul>
</article>