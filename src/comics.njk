---
layout: 'layouts/default.njk'
---

<section>
  <div class="container mx-auto">
    <ul class="grid(& cols-3) gap-10 lg:(gap-16)">
      {% for series in (comics | unique('series')) -%}
        <li>
          <a href="/comics/{{ series.series | slugify }}/" class="mx-auto max-w-xs text-center block">
            <figure>
              <img src="/_assets/img/comics/{{ series.series | slugify }}/series.jpg?v={% version %}" alt="{{ series.series }}" class="mx-auto w-32" />
              <figcaption>
                <strong class="truncate">{{ series.series }}</strong>
                <ul class="text-xs font-mono flex flex-wrap justify-center gap-x-3">
                  {%- for comic in comics %}
                    {%- if comic.series === series.series %}
                      <li>
                        {{ comic.issue }}
                      </li>
                    {%- endif %}
                  {%- endfor %}
                </ul>
              </figcaption>
            </figure>
          </a>
        </li>
      {% endfor -%}
    </ul>
  </div>
</section>