{%- macro x(slot='Heading',class='') -%}
  <h1 class="label mb-8 text-xl leading-none inline-flex items-end md:(pr-2 text-[calc(2.5vw)] leading-none gap-2) {{ class }}" data-label="Title:">
    {{ slot }}
  </h1>
{%- endmacro -%}