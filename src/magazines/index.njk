---
layout: 'layouts/default.njk'
pagination:
  data: magazines.series
  size: 1
  alias: seriesCurr
permalink: "magazines/{{ seriesCurr | slugify }}/"
eleventyComputed:
  title: "{{ seriesCurr }} : Magazines"
---

<section>
  <div class="container mx-auto">
    <h1>{{ seriesCurr }}</h1>
    <ul class="grid(& cols-3) gap-16">
      {%- for item in magazines.items | pluck(seriesCurr, 'series') %}
        <li>
          <figure class="mx-auto w-full max-w-screen-xs">
            <img src="/_assets/img/_comic.jpg" alt="{{ item.series }}, {{ item.issue }}" class="w-full" />
            <figcaption>#{{ item.issue }} / {{ item.release_date }}</figcaption>
          </figure>
        </li>
      {%- endfor %}
    </ul>
  </div>
</section>